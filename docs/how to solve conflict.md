⚠️ 충돌(Conflict) 해결하기

같은 파일의 같은 줄을 여러 명이 동시에 수정하면 발생합니다. 당황하지 말고 아래 순서를 따르세요.

### ① 충돌 확인
git pull 시 'CONFLICT' 메시지가 뜨면, 해당 파일을 직접 엽니다.

### ② 코드 선택
파일 안에 생성된 특수 표식을 보고 최종적으로 남길 코드를 결정합니다.

```
<<<<<<< HEAD (내가 수정한 내용)
print("Hello My World")
=======
print("Hello Our World")
>>>>>>> main (서버에서 가져온 내용)
```

위의 꺽쇠와 등호(<<<<, ====, >>>>) 표식들을 모두 지우고 최종 코드만 남긴 뒤 저장하세요.

### ③ 해결 완료 알리기
수정이 끝났다면 다시 Git 동기화를 수행합니다.

```
> git add [파일명]
> git commit -m "충돌 해결 완료"
> git push origin main
```